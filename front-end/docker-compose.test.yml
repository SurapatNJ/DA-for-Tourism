
version: "3.2"
services:
  front-end:
    restart: always
    command : npm start
    container_name: FrontEnd
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    stdin_open: true
    depends_on:
      - mysite
    networks:
      - db-net

